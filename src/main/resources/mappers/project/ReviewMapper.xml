<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.demo.project.review.ReviewMapper">

    <!-- 테이블 전체 컬럼 -->
    <sql id="postColumns">
        id
        , title
        , writer
        , secret_yn
        , content
        , password
        , original_name
        , save_name
        , upload_path
        , delete_yn
        , created_date
        , modified_date
        , deleted_date
    </sql>

    <!-- 저장 -->
    <insert id="save" parameterType="com.example.demo.project.review.ReviewRequest" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO review (
            <include refid="postColumns" />
        ) VALUES (
            #{id}
            , #{title}
            , #{writer}
            , #{secret_yn}
            , #{content}
            , #{password}
            , #{originalName}
            , #{saveName}
            , #{uploadPath}
            , 0
            , NOW()
            , NULL
            , NULL
        )
    </insert>

    <!-- 수정 -->
    <update id="update" parameterType="com.example.demo.project.review.ReviewRequest">
        UPDATE
            review
        SET
            modified_date = NOW()
            , title = #{title}
            , writer = #{writer}
            , content = #{content}
            , secret_yn = #{secretYn}
            , password = #{password}
        WHERE
            id = #{id}
    </update>

    <update id="updateWithFiles" parameterType="com.example.demo.project.review.ReviewRequest">
        UPDATE
            review
        SET
            modified_date = NOW()
            , title = #{title}
            , writer = #{writer}
            , content = #{content}
            , secret_yn = #{secretYn}
            , password = #{password}
            , original_name = #{originalName}
            , save_name = #{saveName}
            , upload_path = #{uploadPath}
        WHERE
            id = #{id}
    </update>

    <!-- 삭제 -->
    <delete id="deleteById" parameterType="long">
        UPDATE
            review
        SET
            deleted_date = NOW()
            , delete_yn = 1
        WHERE
            id = #{id}
    </delete>

    <!-- 상세정보 조회 -->
    <select id="findById" parameterType="long" resultType="com.example.demo.project.review.ReviewResponse">
        SELECT
            <include refid="postColumns" />
        FROM
            review
        WHERE
            id = #{value}
    </select>

    <!-- 리스트 조회 -->
    <select id="findAll" parameterType="com.example.demo.base.paging.SearchDto" resultType="com.example.demo.project.review.ReviewResponse">
        SELECT
            <include refid="postColumns" />
        FROM
            review
        WHERE
            delete_yn = 0
            <include refid="search" />
        ORDER BY
            id DESC
        LIMIT #{pagination.limitStart}, #{recordSize}
    </select>

    <!-- 게시글 수 카운팅 -->
    <select id="count" parameterType="com.example.demo.base.paging.SearchDto" resultType="int">
        SELECT
            COUNT(*)
        FROM
            review
        WHERE
            delete_yn = 0
            <include refid="search" />
    </select>

    <!-- 게시글 검색 -->
    <sql id="search">
        <!-- 검색 키워드가 있을 때 -->
        <if test="keyword != null and keyword != ''">
            <choose>
                <!-- 검색 유형이 있을 때 -->
                <when test="searchType != null and searchType != ''">
                    <choose>
                        <when test="'title'.equals( searchType )">
                            AND title LIKE CONCAT('%', #{keyword}, '%')
                        </when>
                        <when test="'content'.equals( searchType )">
                            AND content LIKE CONCAT('%', #{keyword}, '%')
                        </when>
                        <when test="'writer'.equals( searchType )">
                            AND writer LIKE CONCAT('%', #{keyword}, '%')
                        </when>
                    </choose>
                </when>

                <!-- 전체 검색일 때 -->
                <otherwise>
                    AND (
                        title LIKE CONCAT('%', #{keyword}, '%')
                        OR content LIKE CONCAT('%', #{keyword}, '%')
                        OR writer LIKE CONCAT('%', #{keyword}, '%')
                    )
                </otherwise>
            </choose>
        </if>
    </sql>
</mapper>